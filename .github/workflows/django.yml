name: Django CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'APP_Django/**'
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/common.yml
    secrets: inherit  # ✅ Secrets 상속 설정
